export declare class EzpAuthorizationService {
  constructor(redirectURI: string, clientID: string);
  clientID: string;
  redirectURI: string;
  oauthUrl: string;
  code: string;
  authURI: URL;
  urlParams: URLSearchParams;
  isAuthorized: boolean;
  accessTokenURL: string;
  codeVerifier: string;
  codeChallenge: string;
  accessToken: string;
  refreshToken: string;
  generateCodeVerifier(): void;
  generateCodeChallenge(codeVerifier: string): Promise<void>;
  buildAuthURI(): void;
  getAccessToken(): Promise<void>;
  refreshTokens(): void;
}
declare const authStore: import("@stencil/store").ObservableMap<{
  codeVerifier: string;
  accessToken: string;
  refreshToken: string;
  isAuthorized: boolean;
  devApi: boolean;
  authApiHostUrl: string;
  redirectUri: string;
}>;
export default authStore;
export declare function sendCodeToParentWindow(): void;
