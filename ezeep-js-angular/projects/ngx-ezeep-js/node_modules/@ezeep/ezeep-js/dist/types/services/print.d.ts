import { /* PrinterProperties, */ PrinterConfig, PrinterProperties } from '../shared/types';
export declare class EzpPrintService {
  constructor(redirectURI: string, clientID: string);
  clientID: string;
  redirectURI: string;
  devApi: boolean;
  printerConfig: PrinterConfig;
  printingApi: string;
  private checkStoredRefreshToken;
  private registerFetchInterceptor;
  getPrinterList(accessToken: string): Promise<any>;
  getConfig(accessToken: string): Promise<any>;
  getPrinterProperties(accessToken: string, printerID: string): Promise<any>;
  getAllPrinterProperties(accessToken: string): Promise<any>;
  printFileByUrl(accessToken: string, fileUrl: string, fileType: string, printerID: string, properties: PrinterProperties, filename?: string, printAndDelete?: boolean): Promise<any>;
  getPrintStatus: () => Promise<any>;
}
declare const printStore: import("@stencil/store").ObservableMap<{
  printers: any[];
  jobID: string;
  printFinished: boolean;
  printApiHostUrl: string;
}>;
export default printStore;
