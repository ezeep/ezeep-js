import { EventEmitter } from '../../stencil-public-runtime';
import { EzpAuthorizationService } from '../../services/auth';
export declare class EzpAuth {
  clientID: string;
  redirectURI: string;
  hidelogin: boolean;
  auth: EzpAuthorizationService;
  authURI: string;
  accessToken: string;
  authCancel: EventEmitter<MouseEvent>;
  printShow: EventEmitter;
  windowObjectReference: any;
  previousUrl: any;
  openSignInWindow(url: string, name: string): void;
  receiveMessage(event: any): void;
  handleCancel: () => void;
  componentWillLoad(): Promise<void>;
  render(): any;
}
